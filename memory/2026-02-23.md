# 2026-02-23

## Website Rebuild Decision
- Mees purchased **youraiworker.nl** on Namecheap
- Rebuilding site with **Next.js + React + Tailwind CSS** (replacing Astro)
- Deploying on **Vercel**
- Contact form emails to: Openclawbot69420@gmail.com
- Remove "Premium · corporate · NL-first" badge from site
- Create a **separate public git repo** for the website only (keep private files out)
- Approach: first get the scaffold + domain + Vercel working, then iterate on content/design

## Contact Form (Gmail SMTP)
- Implemented Next.js API route `/api/contact` using Nodemailer + Gmail SMTP.
- Requires Vercel env vars:
  - `GMAIL_USER`
  - `GMAIL_APP_PASSWORD` (no spaces)
- Confirmed working via live test email.
- Removed debug info from API responses after troubleshooting.

## Typography Preference / Branding Rule
- Do **not** use the em dash character `—` in titles/metadata; use regular hyphen-minus `-` instead.

## Mees Coding Background (Fusey Project)
- Mees works on **Fusey** — two-sided marketplace connecting gaming promoters with sponsors
- Tech stack: TypeScript, React, TanStack (Router/Query/Form/Start), Tailwind CSS 4, oRPC, Kysely, PostgreSQL, Zod 4, pnpm monorepo
- Has detailed AGENTS.md conventions for frontend, backend, and general code style
- This means Mees is more technical than initially assumed — understands modern web dev patterns
- Saved full conventions to memory/mees-coding-conventions.md

## Design Overhaul (earlier today)
- Codex did a premium redesign of the Astro site (Inter font, color system, sticky nav, hamburger menu)
- This work is now being replaced by the Next.js rebuild — design ideas can be carried over

## Ops / Automation
- Set up cron job `hourly-professionalize` (every 1h) to ship one small, safe improvement to `youraiworker-site` (build + commit + push).
- Updated `progress-update-2h` cron prompt to track `youraiworker-site` (old Astro repo updates became noise).

## Codex Auth Reliability
- Codex CLI background runs failed with 401 until device OAuth login was completed.
- Final state: `codex login status` shows **Logged in using ChatGPT**.
- Noted that Codex sandbox environment may lack `npm` and `rg` even when host has them; prefer host-side build checks when needed.

## Website Milestones (youraiworker-site)
- Added pages + nav: `/use-cases`, `/integrations`, `/guides`.
- Added Lucide icons for use cases/guides and upgraded footer layout (incl. LinkedIn/GitHub placeholders).
- Implemented real integration brand logos/colors via `simple-icons` (BrandIcon uses `icon.hex`) with fallbacks where icons are missing.
- Added animated subtle mesh background utility for premium section headers.
- Added detail pages (SSG):
  - `/use-cases/[slug]` (12)
  - `/integrations/[slug]`
  - `/guides/[slug]`
  With centralized content in `src/lib/catalog.ts`.
- Contact page improved with "what happens next" and intake checklist; later adjusted layout so these blocks appear below the form.
- Added `/robots.txt` + `/sitemap.xml`.
- Pricing updated to better fit content: added a mid-tier **Groei** package (3 agents) alongside Starter and Maatwerk.
- Added an Add-ons section with indicative “vanaf” pricing and clarified that customer can supply credentials or we can set up channels/accounts as paid add-ons.

## Target Audience / Positioning (Website Copy)
- Primary audience: companies where OpenClaw agents have real impact (B2B teams)
- Secondary: solopreneurs when it helps
- Focus sectors: agencies, retail/ecom, SaaS, dienstverlening
- Delivery claim: "binnen dagen live" is acceptable, but must be paired with a clear list of what the buyer must provide (access, accounts, API keys/subscriptions).

## Content/UX Requirements (from Mees)
- Keep improving continuously; verify design/copy coherence.
- Contact page requirement: “Wat gebeurt er daarna?” + checklist should appear **below** the form (not beside it).
- Integrations should use actual logos with correct brand colors.

## Hourly Improvements
- `hourly-professionalize` run updated footer social links from dead `#` placeholders to real external URLs and added safe outbound linking attributes (`target=_blank`, `rel=noreferrer`).

## Configurator (Pricing UX)
- Added `/configure` page to select package + add-ons.
- Added ROI/efficiency estimate with sliders: emails/day, leads/week, tickets/week, plus hourly rate.
- Config is saved and passed to contact via query/localStorage; contact form appends configuration to message sent via email.
- UX direction from Mees: keep this very simple for non-technical buyers; show an easy summary and avoid overwhelming/technical JSON.
